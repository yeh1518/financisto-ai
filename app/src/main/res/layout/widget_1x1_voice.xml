<?xml version="1.0" encoding="utf-8"?>
<!--
  1x1 語音記帳 widget：整格就是一顆麥克風，點下去＝開錄（等同桌面捷徑）。
  不放文字：1x1 在多數 launcher 只有 40~60dp，塞字只會兩邊都看不清。
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ai_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/ai_widget_mic"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:adjustViewBounds="true"
        android:contentDescription="@string/ai_voice_shortcut_long"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_ai_mic_shortcut" />
</FrameLayout>
