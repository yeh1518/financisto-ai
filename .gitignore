.idea/**
.gradle/**
build/**
app/build/**
captures/
*.iml
local.properties

# 注意：gradle/wrapper/ 與 gradlew* 是「要進 git」的，別再加回忽略名單。
# wrapper 進 git 才能讓乾淨 clone 直接 ./gradlew 建置（.gradle/ 是建置快取，仍然忽略）。

# --- secrets（API key 等，一律不進版控）---
.env
.env.*
*.key
*.pfx
*.pem
*.keystore
*.jks
secrets/
ai-config.local.*

# --- 個人資料與私人開發筆記（維護者本地留存，不進公開版控）---
local-backups/
*.backup
DEV-BUILD.md
docs/對帳規則.md
tools/restructure-categories.py

# 實驗資料（含個人財務資訊，只留本地）
docs/experiments/on-device-llm-eval/entities.json
docs/experiments/on-device-llm-eval/testset.json
docs/experiments/on-device-llm-eval/result_*.json
docs/experiments/on-device-llm-eval/cloud_result_*.json
docs/experiments/on-device-llm-eval/gemma_failures.json
docs/experiments/on-device-llm-eval/q36_fail.json

# 本地備份設定（個人基建，不進公開 repo）
.backup-manifest

# Release 簽章（絕不進 git）
keystore.properties
*.jks
*.keystore

# 上游新增
CLAUDE.local.md
